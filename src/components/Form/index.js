import React, { PureComponent } from 'react';

class Form extends PureComponent {
  render() {
    return (
      <div>
        Main form component
      </div>
    )
  }
}

export default Form;
